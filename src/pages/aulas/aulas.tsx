import { Grid } from '@mui/material';

export default function Aulas() {
  return (
    <Grid container spacing={-1}>
      <Grid>Class Pageee, componente de tabela, etc... de aulas aqui</Grid>
    </Grid>
  );
}